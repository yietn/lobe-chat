import { Flexbox } from 'react-layout-kit';

const ConfigPanel = () => {
  return (
    <Flexbox
      style={{
        height: '100%',
        overflowY: 'auto',
        width: 260,
      }}
    >
      配置面板
    </Flexbox>
  );
};

export default ConfigPanel;
